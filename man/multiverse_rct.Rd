% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiverseRCT.r
\name{multiverse_rct}
\alias{multiverse_rct}
\title{Run a multiverse of analytic decisions}
\usage{
multiverse_rct(
  data,
  outcome,
  treatment,
  covariates = NULL,
  preprocessing = defaults_prep(),
  model_specs = NULL,
  outcome_type = c("continuous", "binary", "count"),
  parallel = FALSE,
  extract_fun = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Bare outcome variable.}

\item{treatment}{Bare treatment indicator (0/1 or factor).}

\item{covariates}{Optional character vector or one-sided formula
with covariates, e.g. \code{~ age + baseline}.}

\item{preprocessing}{Named list of preprocessing functions.}

\item{model_specs}{Named list of model definitions.  If \code{NULL}, models
are created automatically based on \code{outcome_type}.}

\item{outcome_type}{Type of outcome variable: "continuous", "binary", or "count".}

\item{parallel}{Logical, use \code{future_lapply()} if \code{TRUE}. Can also be a number
specifying the number of worker cores to use.}

\item{extract_fun}{Custom function to extract treatment effects from models.}

\item{...}{Extra args passed to custom \code{fit()} functions.}
}
\value{
A tibble of class \code{"multiverse_rct"}.
}
\description{
Run a multiverse of analytic decisions
}
\examples{
# Create a simple example dataset
set.seed(123)
n <- 30
example_data <- data.frame(
  treatment = rep(c(0, 1), each = n/2),
  covariate = rnorm(n),
  outcome = rnorm(n)
)
example_data$outcome <- example_data$outcome + 0.5 * example_data$treatment

# Basic usage with continuous outcome
mv <- multiverse_rct(
  data = example_data,
  outcome = outcome,
  treatment = treatment,
  covariates = ~ covariate
)

# Summarize results
summary(mv)

\dontrun{
# Examples using the included rct_data dataset

# With continuous outcome
mv <- multiverse_rct(
  data = rct_data,
  outcome = outcome_score,
  treatment = treatment_group,
  covariates = ~ baseline + age
)

# With binary outcome
mv_binary <- multiverse_rct(
  data = rct_data,
  outcome = success,
  treatment = treatment_group,
  outcome_type = "binary"
)

# With custom preprocessing
my_preprocessing <- defaults_prep()
my_preprocessing$log_transform <- function(df) {
  df$outcome_score <- log(df$outcome_score + 1)
  return(df)
}

mv_custom <- multiverse_rct(
  data = rct_data,
  outcome = outcome_score,
  treatment = treatment_group,
  preprocessing = my_preprocessing
)
}
}
